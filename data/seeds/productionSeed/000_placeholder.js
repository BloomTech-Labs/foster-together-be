// Probably don't want to seed database with anything
// except a temporary admin user to create their admin accounts with,
//and will only do that once, so for now set this to do nothing

exports.seed = function() {
  return
}
